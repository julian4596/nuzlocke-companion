version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${APP_PORT:-5173}:5173"
    restart: unless-stopped
    container_name: nuzlocke-companion-${ENV_NAME:-main}
